export const metadata = {
  title: 'Platform Admin',
  description: 'Owner administration area for the restaurant automation gateway.',
};

export default function PlatformAdminRootLayout({ children }) {
  return children;
}
