export const metadata = { title: 'Gateway Website | Platform Admin' };

export default function GatewayWebsitePlaceholderPage() {
  return (
    <section className="rounded-lg border border-neutral-200 bg-white p-6 shadow-sm">
      <p className="text-xs font-semibold uppercase tracking-wide text-emerald-700">Coming later</p>
      <h1 className="mt-2 text-2xl font-semibold text-neutral-950">Gateway Website</h1>
      <p className="mt-3 max-w-2xl text-sm leading-6 text-neutral-600">
        Platform website content management will live here in a later batch. For now, the public gateway remains edited
        in code and gateway lead capture remains the only working platform workflow.
      </p>
    </section>
  );
}
