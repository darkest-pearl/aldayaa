import OrderCancelClient from './OrderCancelClient';

export const metadata = { title: 'Cancel Order | Demo Restaurant' };

export default function OrderCancelPage() {
  return <OrderCancelClient />;
}
